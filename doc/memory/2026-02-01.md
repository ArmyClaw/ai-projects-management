# 2026-02-01 记忆日志

## Army Yorozuya网站部署修复经验

### 问题诊断与修复
- **问题**: 网站无法访问，返回连接超时或错误
- **诊断**: 发现nginx配置错误，root目录指向`/usr/share/nginx/html`而非`/usr/share/nginx/html/army-yorozuya`
- **修复**: 修改nginx配置文件，将root路径更正为`/usr/share/nginx/html/army-yorozuya`，然后重新加载nginx服务
- **验证**: 通过curl命令确认HTML和JavaScript文件都能正常访问

### 技术要点
1. **nginx配置**: 部署React应用时必须确保nginx root路径与实际文件部署路径匹配
2. **文件结构**: React构建后的文件位于`/usr/share/nginx/html/army-yorozuya/`目录下
3. **容器部署**: 使用Docker部署时要特别注意容器内文件路径与nginx配置的一致性
4. **验证方法**: 使用curl命令验证HTML和JS文件的可访问性

### 最终状态
- 网站正常运行在 http://localhost
- 终端风格界面正常显示
- 字符艺术图标正确渲染
- 三个技术章节内容完整展示
- 容器名称: army-yorozuya-nginx